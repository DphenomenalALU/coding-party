# Canvas contribution comment templates

The assignment requires each student to report the tasks they completed or significantly contributed to. Copy only your own section into your Canvas submission comment, then adjust the verification sentence to match what you personally checked.

## Ibrahim Salami (@DphenomenalALU)

I completed tasks 1–9 and 50. I set up and cleaned the Vite React + TypeScript app, created and integrated `TeamDashboard`, and completed the final dashboard integration. My commits include `ac9477d`, `68b586a`, `f2e35a7`, `4293911`, `7d4489f`, and `371d639`. I verified the production build and lint checks and manually tested score controls, adding a member, status toggling, filtering, and search.

## Evander Manna (@evand3rr)

I completed tasks 10–20. I created the reusable `MemberCard` component, typed its props, added conditional and optional rendering, default values, and mapped typed member data. My commits include `23f765f` and `5dc425d`. I verified my work through the final build and lint checks.

## Sonia Etuhoko (@soniaetuhoko)

I completed tasks 21–30. I added component-specific card styling, class selectors, active/inactive styles, responsive media queries, and the dashboard grid layout. My commits include `10feae0`, `6266223`, and `5a19adc`. I verified my work through the final build and lint checks.

## Josiane MUKESHIMANA (@Josiane705)

I completed tasks 31–35 and 47. I implemented typed team score state with functional increment/decrement updates, the zero floor, and member status toggling. My commits include `2fa5558`, `648391e`, `4642f6e`, and `71bde44`. I verified my work through the final build and lint checks.

## Irene Winnie (@Winnie-Irene)

I completed tasks 36–40 and 43. I implemented the controlled member-name form, typed change and submit events, `preventDefault()`, validation, and adding new members. The final integration added visible submission feedback. My commits include `d5c9a97` and `babcfd7`. I verified my work through the final build and lint checks.

## Rwigenza Niyoyandemye (@Rwigenzadavy)

I completed tasks 41–42, 44–46, and 48–49. I defined the typed `Member` interface, moved members into typed state, connected add/remove callbacks, and implemented status filtering and name search. The final integration made the search field fully controlled. My commits include `39e1880`, `aae1a37`, `147d46e`, `cd6f67a`, `0cc9cb3`, and `370ba80`. I verified my work through the final build and lint checks.
